---
ahoyapi: v2
commands:
  run:
    usage: Run Jupyter notebooks with no password.
    cmd: |
      poetry run jupyter notebook --notebook-dir=.notebooks --NotebookApp.token='' --NotebookApp.password=''
